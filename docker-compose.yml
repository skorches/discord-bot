services:
  discord-bot:
    build: .
    container_name: discord-music-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DISCORD_PREFIX=${DISCORD_PREFIX:-!}
    # Optional: Uncomment to mount logs directory
    # volumes:
    #   - ./logs:/app/logs
    # Health check (optional)
    healthcheck:
      test: ["CMD", "python", "-c", "import discord; import yt_dlp; print('ok')"]
      interval: 60s
      timeout: 15s
      retries: 3

